<h1>All Projects</h1>

<% @projects.each do |project| %>
  <section id = "project-<%=project.id%>">
    <h2><%= project.name %></h2>
    <h2>Material: <%= project.material %></h2>
  </section>
<% end %>
